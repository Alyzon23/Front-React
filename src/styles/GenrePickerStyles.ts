import { StyleSheet } from 'react-native';

const GenrePickerStyles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: '#4caf50',
    borderRadius: 8,
    marginBottom: 20,
    backgroundColor: '#ffffff',
  },
  picker: {
    height: 60,
    color: '#388e3c',
  },
});

export default GenrePickerStyles;